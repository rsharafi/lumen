Pillow>=10.0
numpy>=1.26

# The window, the OpenGL context, and the event loop. 2.5.2 is the floor
# because `pygame.Window` and its `allow_high_dpi` flag arrived there, and
# without high-DPI the game draws at window points and gets stretched over
# the panel by the compositor.
pygame-ce>=2.5.2

# The renderer. There is exactly one, and this is it.
moderngl>=5.8
